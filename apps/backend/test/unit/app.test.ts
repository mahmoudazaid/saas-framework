// apps/backend/test/unit/app.test.ts
describe('App', () => {
  it('should be defined', () => {
    expect(true).toBe(true);
  });

  it('should have basic math working', () => {
    expect(2 + 2).toBe(4);
  });
});
